import { useFetch } from "../../hooks";import { getLeaderboard } from "../../services/pointsService";import Loading from "../../components/common/Loading";import Card from "../../components/common/Card";import LeaderboardTable from "../../components/tables/LeaderboardTable";import StatusState from "../../components/common/StatusState";
export default function LeaderboardPage(){const {data,loading,error,refetch}=useFetch(getLeaderboard);return <div className="page"><p className="eyebrow">Recognition</p><h1 className="title mt-1">Leaderboard</h1><p className="muted mt-2">Campus participation, ranked by Kortex points.</p><Card className="mt-6 overflow-hidden">{loading?<Loading/>:error?<StatusState type="error" message={error} onRetry={()=>void refetch()}/>:!data?.length?<StatusState type="empty" message="No leaderboard entries are available yet."/>:<LeaderboardTable entries={data}/>}</Card></div>}